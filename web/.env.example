# Server only (Render). When all three are set, the booking form sends via Resend.
# RESEND_API_KEY=
# BOOKING_FROM_EMAIL=booking@jcapturelab.com
# BOOKING_TO_EMAIL=you@example.com

# Optional: Postgres when you add a database.
# DATABASE_URL=
